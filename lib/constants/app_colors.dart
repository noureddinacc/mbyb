import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(255, 8, 128, 12);
  static const Color secondary = Color.fromARGB(255, 27, 138, 25);
  static const Color background = Colors.white;
  // add more as needed
}
